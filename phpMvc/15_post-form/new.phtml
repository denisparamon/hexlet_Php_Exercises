<form action="/courses" method="post">
    <div>
        <label for="paid">Paid:</label>
        <select name="course[paid]" id="paid">
            <option value="">Select...</option>
            <option value="true" <?= isset($course['paid']) && $course['paid'] === 'true' ? 'selected' : '' ?>>Yes</option>
            <option value="false" <?= isset($course['paid']) && $course['paid'] === 'false' ? 'selected' : '' ?>>No</option>
        </select>
        <?php if (isset($errors['paid'])): ?>
            <div class="error"><?= htmlspecialchars_decode($errors['paid']) ?></div>
        <?php endif; ?>
    </div>

    <div>
        <label for="title">Title:</label>
        <input type="text" name="course[title]" id="title" value="<?= htmlspecialchars($course['title'] ?? '') ?>">
        <?php if (isset($errors['title'])): ?>
            <div class="error"><?= htmlspecialchars_decode($errors['title']) ?></div>
        <?php endif; ?>
    </div>

    <button type="submit">Create Course</button>
</form>
